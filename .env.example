# ClawdBot Python - Environment Variables Example
# SECURITY: Copy to .env and NEVER commit .env file with real keys!

# ================================================================
# LLM Configuration (REQUIRED for agent functionality)
# ================================================================

# Model to use (provider/model-name format)
# The API key is automatically extracted based on provider
# Examples:
#   - anthropic/claude-3-5-sonnet (requires ANTHROPIC_API_KEY)
#   - openai/gpt-4 (requires OPENAI_API_KEY)
#   - google/gemini-2.5-flash (requires GOOGLE_API_KEY)
LLM_MODEL=anthropic/claude-3-5-sonnet

# OR use provider-specific keys (for flexibility)
# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI GPT
OPENAI_API_KEY=sk-your-key-here

# Google Gemini (NEW!)
GOOGLE_API_KEY=your-google-api-key

# AWS Bedrock (NEW! - uses AWS credentials)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# Ollama (NEW! - local, no API key needed - just run: ollama serve)

# Channel Tokens (Optional - for testing channels)
TELEGRAM_BOT_TOKEN=
DISCORD_BOT_TOKEN=
SLACK_BOT_TOKEN=

# Application Settings
CLAWDBOT_ENV=development
CLAWDBOT_LOG_LEVEL=INFO
CLAWDBOT_PORT=18789
CLAWDBOT_WEB_PORT=8080

# Security Settings
CLAWDBOT_ALLOWED_ORIGINS=http://localhost:8080
CLAWDBOT_MAX_SESSIONS=100
CLAWDBOT_SESSION_TIMEOUT=3600

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit this file with real API keys
# 2. Use strong, unique keys for production
# 3. Rotate keys regularly
# 4. Use secrets management in production (AWS Secrets Manager, etc.)
# 5. Limit API key permissions to minimum required
